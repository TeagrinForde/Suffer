import React from 'react';
// import { useQuery } from '@apollo/client';

// import { QUERY_PROFILES } from '../utils/queries';

const Home = () => {
  return (
    <div className="center text-white bg-black fullheight">SHOW ME</div>
  );
};

export default Home;
